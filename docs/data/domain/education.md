# Education

Model scores and forecasts remain INFERRED with model/version, population, geography, time horizon, scale and limitations. Do not label a resilience score employment probability without an approved empirical mapping. Synthetic tests and planned LOSO/RMSE/calibration analyses are not measured results. Empirical metrics require identified real datasets, splits, reproducible evaluation and scope; statistical labels require the actual method and assumptions. `examples/education.json` is an explicitly synthetic model estimate.

Host acceptance: test the stated domain invariants at the actual consumer boundary. The general validator does not automatically implement all domain-specific rules.
