# Methodological Documentation & Analytics Specifications
**Studievalg-AI — Analytical Framework & Model Architecture (v2026.5 Methodology Correctness)**

---

## 1. System Overview

Studievalg-AI is a deterministic, data-first decision-support system. Numerical recommendation components are calculated by explicit code and supplied datasets; an LLM is not the source of numerical truth.

The system must distinguish three epistemic layers:

1. **Observed** — directly measured values from a named source dataset.
2. **Derived / crosswalk** — deterministic transformations or values transferred through a documented classification mapping.
3. **Model-based** — estimates produced from explicit assumptions or formulas.

A source registry is not itself evidence for a programme. A programme-level claim is only evidence-backed when a concrete source-to-claim relationship has been established.

---

## 2. Canonical Data Architecture & Provenance

```text
Source dataset
    ↓
Ingestion + integrity validation
    ↓
Canonical programme / occupation mapping
    ↓
Metric transformation / model calculation
    ↓
Recommendation scoring
    ↓
Programme-specific evidence and provenance
    ↓
UI explanation
```

Every important programme-level metric should eventually have:

- source organisation
- dataset/table
- observation period
- raw metric
- transformation
- coverage
- epistemic status
- confidence

Missing provenance must be reported as `UNKNOWN` or `PROVENANCE_REQUIRED`; it must never be upgraded merely because a source appears in the global evidence registry.

The machine-readable audit is generated by:

```text
etl/build_data_provenance_report.py
```

and written to:

```text
data/DATA_PROVENANCE_REPORT.json
```

---

## 3. Metric Definitions

| Metric | Status | Definition |
|---|---|---|
| `automation_risk` | Model / crosswalk | Estimated occupational task exposure to direct AI substitution. Not a Danish observed job-loss rate. |
| `augmentation_potential` | Model / crosswalk | Estimated potential for AI to augment human work. |
| `ai_resilience` | Derived model index | `clamp(0.75 * (1 - automation_risk) + 0.25 * augmentation_potential, 0.1, 1.0)`. This is a model indicator, not a forecast. |
| `labour_demand` | Provenance required | Must identify its actual raw source, population, period and transformation before being described as empirical programme-level demand. |
| `salary_growth` | Provenance required | Must identify its actual raw source, population, period and transformation before being described as empirical programme-level salary growth. |

`automation_exposure` is not a separate empirical metric and must not be displayed as an independently measured variable.

The canonical AI-resilience score gives 75% weight to the inverse automation risk
and 25% weight to augmentation potential. Internal values are bounded to 0.1–1.0;
the web UI displays the rounded value on a 0–100 scale. The implementation and
regression tests are maintained in `web/src/lib/domainScoring.ts`,
`agents/multi_agent_engine.py`, `web/src/__tests__/algorithm.test.ts` and
`tests/test_scoring_contract.py`. A change to this formula is a domain change and
must update all four locations together.

---

## 4. Occupation Crosswalk

DISCO-08 and O*NET/other occupation taxonomies can provide useful task-level evidence, but a crosswalk from an occupation to a Danish education programme is not equivalent to an observed education-level labour-market outcome.

Any such mapping must state:

- mapping source
- mapping method
- whether the mapping is official or heuristic
- confidence

Where one programme plausibly leads to several occupations, a single occupation should not be presented as the complete career outcome without qualification.

---

## 5. Recommendation Score

The recommendation score contains explicit components for:

- `interest_fit`
- `ai_resilience`
- `labour_demand`
- `salary_growth`
- `location_fit`

The weights are normalized before scoring. `interest_fit` is a meaningful component and should influence ranking unless the product explicitly changes the preference model.

Interest matching is currently a deterministic lexical/subject-alignment heuristic. It must not be described as a psychometric assessment or validated personality model.

Location matching distinguishes:

- `STRUCTURED`
- `TITLE_FALLBACK`
- `UNKNOWN`

A title fallback has lower confidence than structured location data.

---

## 6. Evidence Engine

The evidence engine contains a registry of candidate sources with metadata weights. These weights express source-priority configuration only.

They do **not** constitute:

- programme-level evidence quality
- statistical consensus
- probability that a source supports a claim
- validation of every programme

Programme-level evidence must be attached to concrete claims. `programme_evidence_status = NOT_ESTABLISHED` is valid when such a relationship has not been established.

---

## 7. Econometric Validation Status

The repository previously contained a TWFE/DiD routine whose dependent variable was constructed as:

```text
log((KOT threshold × constant) + (platform labour_demand × constant))
```

This is not an independently observed labour-market outcome because it contains a platform-generated model metric. It therefore cannot validate that same metric independently.

That pseudo-validation has been removed.

`etl/reproduce_econometric_results.py` now produces **descriptive KOT diagnostics only** and explicitly reports that no causal/econometric validation is performed.

Observed KOT admission data may still be used for genuine descriptive analysis. Any future causal model must use an independently observed outcome and clearly document treatment, control, identification assumptions and pre-trend diagnostics.

---

## 8. Scenario Simulation

The scenario engine produces model-based simulations under explicit macro assumptions. Percentiles from Monte Carlo simulation are simulation uncertainty ranges, not empirical confidence intervals.

They must be described as:

> model uncertainty / simulation range

and not as a statistical confidence interval for an observed population parameter.

---

## 9. Data Quality Labels

The UI and API should distinguish:

- `OBSERVED`
- `DERIVED`
- `CROSSWALK`
- `MODEL`
- `PROVENANCE_REQUIRED`
- `UNKNOWN`

This prevents a model-generated score from being visually presented as an observed fact.

---

## 10. Limitations

- AI metrics are model-based and/or occupation-crosswalk estimates.
- O*NET is US occupational data and requires a documented crosswalk before Danish interpretation.
- KOT admission statistics describe admissions/applicant selection, not labour-market demand.
- Salary and employment metrics must not be presented as programme-specific facts unless their underlying population and mapping are documented.
- Recommendations are decision support, not deterministic predictions or professional career advice.
- The system must expose uncertainty and missing provenance rather than inventing precision.
